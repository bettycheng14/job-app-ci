# Staging environment — placeholder values only. Real secrets live in Jenkins credentials.
NODE_ENV=staging

# Auth service
JWT_SECRET=staging_demo_secret_key
JWT_EXPIRES_IN=7d

# JobApp service
# GCS_BUCKET_NAME intentionally absent — local file storage used in staging
