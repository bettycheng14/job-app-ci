# Production environment — placeholder values only. Real secrets live in Jenkins credentials.
NODE_ENV=production

# Auth service
JWT_SECRET=prod_demo_secret_key
JWT_EXPIRES_IN=7d

# JobApp service
# GCS_BUCKET_NAME=
GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/gcp-sa-key
